{
    "name": "spindle/collection",
    "description": "The fastest collection(array_map, array_reduce, ..) wrapper",
    "type": "library",
    "license": "CC0-1.0",
    "authors": [
        {
            "name": "Hiraku NAKANO",
            "email": "hiraku@tojiru.net"
        }
    ],
    "autoload": {
        "files": ["src/Collection.php"]
    },
    "require": {
        "php": ">=5.6"
    },
    "require-dev": {
        "phpunit/phpunit": "^4.8",
        "squizlabs/php_codesniffer": "^3.0"
    },
    "scripts": {
        "test": "phpunit --colors=always",
        "dbg": "phpdbg -qrr vendor/bin/phpunit --colors=always",
        "fmt": "phpcbf --colors --standard=PSR2 src",
        "lint": "phpcs --colors --standard=PSR2 src"
    }
}
